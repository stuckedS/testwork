**# Создаем виртуальную переменную**
python -m venv venv

**# Активируем**
source venv/bin/activate      # Linux / WSL / macOS
**# или**
venv\Scripts\activate         # Windows

**# Устанавливаем либы**
pip install -r requirements.txt

**После, можно запускать**
